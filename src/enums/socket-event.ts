export enum SocketEvent {
  AUTHENTICATE = 'authenticate',
  AUTHENTICATED = 'authenticated',
  UNAUTHORIZED = 'unauthorized',
  JOIN_ROOM = 'join room',
  LEAVE_ROOM = 'leave room',
  CLIENT_SEND = 'client send',
  CLIENT_RECIEPT = 'client reciept',
}
